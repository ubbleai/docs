# Web integration

When you request an [identity verification](../identity-verification/index.md), you'll receive a `verification_url` in the response. Redirect your user to this URL so they can perform the verification.

To ensure a smooth user experience, you can control how the redirection is handled.
