# Redirect the user

The create an attempt response contains a `_links.verification_url.href` field. 
To test the web flow, open the `verification_url` on your browser and follow the instructions.
To complete your integration, you will have to redirect applicants to our web application (see [Web integration](../web-integration/index.md)) or to use our SDK integration.
